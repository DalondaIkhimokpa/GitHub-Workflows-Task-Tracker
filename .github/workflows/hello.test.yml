name: Hello Test
on:
    workflow_dispatch:

jobs:
    print-hello-world:
        runs-on: ubuntu-22.04
        steps:
            - name: Print Hello World Test
              run: echo "Hello, World!"
            