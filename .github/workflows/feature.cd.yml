# .github/workflows/simple-cd.yml

name: Feature CD (Login)

on:
  workflow_run:
    workflows: ["Feature CI (Login)"]  # MUST MATCH THE CI WORKFLOW NAME
    types:
      - completed

jobs:
  run-cd:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Simulate deployment
        run: echo "🚀 CD ran after CI passed on feature/login"
