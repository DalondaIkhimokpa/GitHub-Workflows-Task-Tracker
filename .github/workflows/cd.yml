name: CD - Feature Login

on:
  workflow_run:
    workflows: ["CI - Feature Login"]
    types: [completed]

jobs:
  deploy-feature:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Simulate deployment for feature/login
        run: |
          echo "Deploying FEATURE/LOGIN branch..."
          echo "✅ Feature login deployment complete!" > deploy-feature.log

      - name: Show deployment log
        run: cat deploy-feature.log
