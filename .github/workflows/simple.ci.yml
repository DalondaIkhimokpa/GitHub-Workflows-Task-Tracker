name: Simple CI

on:
  push:
    branches: [feature/login]
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print a message
        run: |
          echo "✅ Simple CI triggered for feature/login"
          echo "Branch: ${{ github.ref_name }}"
