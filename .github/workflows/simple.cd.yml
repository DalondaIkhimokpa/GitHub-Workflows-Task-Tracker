name: Simple CD

on:
  workflow_run:
    workflows: ["Simple CI"]
    types: [completed]

jobs:
  run-cd:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'feature/login' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Simulate deployment
        run: echo "🚀 Simple CD triggered for feature/login branch"
